import 'package:flutter/material.dart';

class AppColors {
  // Fresh Teal – Primary brand color
  static const Color primary = Color.fromARGB(255, 0, 150, 136);

  // Vibrant Coral – Accent / CTA color
  static const Color accent = Color.fromRGBO(255, 87, 34, 1);

  // Soft Ivory – Clean background
  static const Color background = Color.fromARGB(255, 245, 245, 245);

  // Mint Green – Cards and containers
  static const Color card = Color.fromARGB(255, 224, 242, 241);

  // Charcoal Gray – Readable text
  static const Color text = Color.fromARGB(255, 33, 33, 33);
}

class Dimens {
  static const double padding = 18.5;
  static const double radius = 14.0;
}
